# 需求追踪矩阵

## 模块总览（上层）

| 模块编号 | 模块名称 | 优先级 | 依赖模块 | 状态 | 备注 |
| --- | --- | --- | --- | --- | --- |
| MOD-001 | | P0/P1/P2 | — | 未开始 | |

> 状态值：`未开始` → `需求(5a)` → `设计(5b)` → `数据/API(5c)` → `实现中(5d)` → `测试(5e)` → `完成`

## 当前模块明细（下层）

> 仅展开当前正在执行的模块。完成的模块折叠其明细。

**当前模块：** <!-- MOD-XXX 模块名 -->

| Slice 编号 | 需求描述 | 对应需求ID | 测试用例 | 实现状态 | 验收状态 |
| --- | --- | --- | --- | --- | --- |
| MOD-XXX-S01 | | REQ-XXX | | 未开始 | |

> Slice 实现状态：`未开始` → `红(failing test)` → `绿(passing)` → `重构完成`
> 验收状态：`未验收` → `已验收`
